{
  "name": "rio-restro-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node dev.cjs",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "deploy": "npm run build && wrangler deploy"
  }
}
